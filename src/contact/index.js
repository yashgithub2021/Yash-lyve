const { contact } = require("./contact.modal");
const contactController = require("./contact.controller");
const contactRoute = require("./contact.router");

module.exports = { contact, contactController, contactRoute };
